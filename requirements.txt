# Capacity Management — Python dependencies

# Core ML / forecasting
# pmdarima  : pure-Python Auto-ARIMA (wraps statsmodels) — no compiler needed on Windows
# statsmodels: STL seasonal decomposition + ExponentialSmoothing ensemble
pmdarima>=2.0.4
statsmodels>=0.14.0

# Data manipulation
pandas>=2.1.0
numpy>=1.26.0

# Elasticsearch
elasticsearch>=8.10.0

# Visualization
matplotlib>=3.8.0
